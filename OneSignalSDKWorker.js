// Service worker for OneSignal web push. It has to sit at the site root so its
// scope covers every page. Nothing else belongs in here.
importScripts('https://cdn.onesignal.com/sdks/web/v16/OneSignalSDK.sw.js');
